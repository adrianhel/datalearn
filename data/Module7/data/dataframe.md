## 7.6.1 Структура данных Dataframe

### [Назад в Модуль 7 ⤶](/data/Module7/readme.md)

>**DataFrame** – это абстракция, представляющая собой распределённую коллекцию данных, организованных в виде 
> именованных столбцов, аналогично таблице в реляционной базе данных или датафрейму в таких языках, как Python (Pandas) и R.  

DataFrame обеспечивает высокоуровневый API для работы с большими объёмами данных, реализуя эффективные механизмы 
хранения, обработки и анализа структурированных и полу-структурированных данных.  

### Основные понятия
- **Столбец (Column)** — именованный набор данных одного типа, аналогичный столбцу в таблице базы данных.  
- **Строка (Row)** — набор значений, соответствующих одному элементу данных, аналогичный строке в таблице базы данных.  
- **Схема (Schema)** — структура, определяющая имена столбцов и их типы данных.  
- **Дистрибуция (Distribution)** — данные в DataFrame распределены по множеству узлов кластера для параллельной обработки.  

**DataFrame** можно рассматривать как двумерную таблицу, где каждая запись (строка) состоит из набора значений, 
ассоциированных с именами столбцов. Для каждого столбца задан тип данных, что позволяет применять к данным 
типизированные операции и оптимизации.  

```txt
+----------+-------+--------+
| name     | age   | salary |
+----------+-------+--------+
| "Иван"   | 30    | 50000  |
| "Мария"  | 25    | 60000  |
| "Петр"   | 29    | 70000  |
+----------+-------+--------+
```

## 7.6.2 Типы данных
Числовые: `IntegerType`, `LongType`, `FloatType`, `DoubleType`  
Строковые: `StringType`  
Булевы: `BooleanType`  
Дата и время: `DateType`, `TimestampType`  
Сложные типы: `ArrayType`, `MapType`, `StructType`  

## 7.6.3 Создание DataFrame
**DataFrame** может быть создан из различных источников данных, включая:  
- Внешние файлы (CSV, Parquet, JSON, Avro и др.)  
- Таблицы в реляционных базах данных (через JDBC)  
- RDD (Resilient Distributed Dataset)  
- Локальные коллекции  

#### Пример создания DataFrame из RDD

```python
from pyspark.sql import SparkSession, Row

spark = SparkSession.builder.getOrCreate()
rdd = spark.sparkContext.parallelize([
    Row(name="Иван", age=30, salary=50000),
    Row(name="Мария", age=25, salary=60000)
])
df = spark.createDataFrame(rdd)
df.show()
```
                  
#### Пример создания DataFrame из файла CSV

```python
df = spark.read.csv("employees.csv", header=True, inferSchema=True)
df.printSchema()
df.show()
```
                  
## 7.6.4 Манипуляции и операции над DataFrame
- Выбор столбцов (select): `df.select("name", "salary")`
- Фильтрация строк (filter или where): `df.filter(df.age > 25)`
- Агрегация (группировка): `df.groupBy("department").avg("salary")`
- Сортировка: `df.orderBy(df.salary.desc())`
- Добавление новых столбцов: `df.withColumn("age_plus_ten", df.age + 10)`
- Объединение DataFrame (join): `df1.join(df2, df1.id == df2.id)`
- Удаление дубликатов: `df.dropDuplicates()`
- Удаление столбцов: `df.drop("salary")`

> 💡 Операции над **DataFrame** определяют логический план выполнения, который оптимизируется и исполняется только 
> при необходимости получить результат (например, при вызове методов `show()`, `collect()`, `count()`). Такой подход 
> позволяет минимизировать избыточные вычисления и повысить производительность.  

## 7.6.5 Схема и типизация
Схема DataFrame может быть как явно задана пользователем, так и автоматически выведена при чтении данных.  
Явная типизация обеспечивает строгий контроль типов, предотвращая ошибки на этапе выполнения.  

```python
from pyspark.sql.types import StructType, StructField, StringType, IntegerType

schema = StructType([
    StructField("name", StringType(), True),
    StructField("age", IntegerType(), True),
    StructField("salary", IntegerType(), True)
])

df = spark.read.csv("employees.csv", header=True, schema=schema)
df.printSchema()
```

## 7.6.6 SQL-подобные операции
DataFrame поддерживает выполнение SQL-запросов. Для этого DataFrame регистрируется как временная таблица, после чего 
к ней могут быть применены SQL-операторы.  

```python
df.createOrReplaceTempView("employees")
result = spark.sql("SELECT name, salary FROM employees WHERE age > 25")
result.show()
```

## 7.6.7 Примеры практического применения DataFrame
Аналитика больших данных и построение отчётов по ключевым метрикам бизнеса  
Обработка и очистка данных для последующего машинного обучения  
Интеграция и объединение данных из различных источников  
Подготовка данных для визуализации и анализа  
Реализация сложных ETL-процессов (Extract, Transform, Load)  

## 7.6.8 Преимущества DataFrame
- Высокоуровневый API с поддержкой широкого набора операций  
- Оптимизация выполнения через катализатор запросов и физический планировщик  
- Автоматическое управление распределением и партиционированием данных  
- Интеграция с различными форматами и источниками данных  
- Поддержка многоязычных интерфейсов (Python, Scala, Java, R)  

## 7.6.9 Ограничения DataFrame
- Ограниченная поддержка низкоуровневых операций по сравнению с RDD  
- Сложности при работе с неструктурированными или сильно вложенными данными  
- Зависимость от корректного определения схемы данных  

## 7.6.10 Формулы и выражения
Многие операции реализуются с помощью выражений, например:
- Агрегирование: `df.groupBy("department").sum("salary")`
- Вычисление новых столбцов: `df.withColumn("bonus", df.salary * 0.1)`
- Фильтрация по условию: `df.filter(df.salary > 50000)`

## 7.6.11 